#PN (JOBS-V3)
#DEPLOYMENT
fullnameOverride: gorush
nameOverride: gorush
replicaCount: 40
tier: frontend
namespace: gorush

image:
  repository: asia-southeast1-docker.pkg.dev/bustling-sunset-220007/newswav-gorush/newswav-gorush
  tag: nw-v1.18

resources:
  limits:
    memory: 500Mi
    cpu: "500m"
  requests:
    memory: 10Mi
    cpu: "5m"

readinessProbe:
  httpGet:
    path: /healthz
    port: 8088
  initialDelaySeconds: 5
  periodSeconds: 30
  timeoutSeconds: 10

livenessProbe:
  httpGet:
      path: /healthz
      port: 8088
  initialDelaySeconds: 5
  periodSeconds: 60
  timeoutSeconds: 30

envFrom:
  - configMapRef:
      name: gorush-config
  - secretRef:
      name: gorush-gcpsm

appContainerPort: 8088

#SERVICE
service:
  enabled: true
  type: NodePort
  port: 80

#ingress
ingress:
  enabled: true
  annotations:
    kubernetes.io/ingress.global-static-ip-name: new-gorush-ingress-ip
  tls: 
   -  secretName: ssl-wildcard-newswav
      hosts:
        - gorush.newswav.com

  hosts:
  - host: gorush.newswav.com
    paths:
      - path: /
        pathType: Prefix